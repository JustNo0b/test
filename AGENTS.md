# AGENTS.md

## Cursor Cloud specific instructions

This is a newly initialized repository with no application code, dependencies, or services yet. The repo currently contains only a single placeholder file (`test rucsor`).

- **No build system, package manager, or language runtime** is configured.
- **No services** need to be started.
- **No lint, test, or build commands** are available.

As the codebase grows, update this section with:
- How to install dependencies and which package manager to use.
- How to run lint, tests, and builds.
- How to start development servers and any required backing services.
- Any non-obvious caveats discovered during setup.
